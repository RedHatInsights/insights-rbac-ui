/**
 * V2 groups handler factory.
 * Currently uses V1 API — will switch to V2 when the API ships.
 */
export { createGroupsHandlers, type GroupsHandlerOptions } from '../../../shared/data/mocks/groups.handlers';
