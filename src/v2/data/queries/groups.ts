// V2 groups shim — re-exports from the shared data layer.
// There is no V2 groups API yet; when one ships, add V2-specific
// hooks here and the consumers' import paths won't change.
export * from '../../../shared/data/queries/groups';
